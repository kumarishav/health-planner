import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.healthplanner',
  appName: 'Health Planner',
  webDir: 'www'
};

export default config;
